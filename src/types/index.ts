export interface Book {
  id: string;
  title: string;
  author: string;
  coverImage: string;
  price: number;
  description: string;
  category: string;
}

export interface User {
  id: string;
  email: string;
  name: string;
} 